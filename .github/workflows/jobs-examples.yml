name: jobs examples

on:
   push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step do job1
        run: echo Hello, world! Build.


  job2:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: step do job2
        run: echo Hello, world! Deploy.
